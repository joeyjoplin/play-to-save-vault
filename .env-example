# Toggle mock vault (localStorage) vs real chain integration
VITE_USE_MOCK_VAULT=<set false for onchain operations>

# Minimum XLM required in the vault to unlock the game
VITE_MIN_VAULT_XLM=<minimal amount to play>

# === Future real chain vars (Soroban / Stellar / Base) ===
VITE_STELLAR_RPC_URL=https://soroban-testnet.stellar.org
VITE_FEE_VAULT_CONTRACT=C...S
VITE_RESERVE_ADDRESS=C..C
VITE_TOKEN_DECIMALS=7
VITE_STELLAR_NETWORK_NAME=TESTNET